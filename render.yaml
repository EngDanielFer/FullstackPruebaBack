services:
  - type: web
    name: backend-autos
    env: python
    buildCommand: ""
    startCommand: python app.py
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:postgres@localhost/bd_autos